import SwiftUI

struct PrayerPlayerView: View {
    var body: some View {
        VStack(spacing: 16) {
            Text("🪔")
            Text("Prayer Player")
            Text("Repetition tracking and live activity updates belong here.")
        }
        .padding(24)
    }
}

